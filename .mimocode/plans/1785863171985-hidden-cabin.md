# Plan: Replace page.tsx with shadcn sidebar layout

## Problem
Current `src/app/page.tsx` uses `PremiumSidebar` + `DashboardLayout` with weather-specific props. User wants to replace it with the shadcn sidebar system using the 4 components in `components/sidebar/`.

## Key issues found
1. `app-sidebar.tsx` imports `TeamSwitcher` (missing) — **remove per user**
2. `app-sidebar.tsx` imports from wrong paths (`@/components/nav-main` instead of `@/components/sidebar/nav-main`)
3. User's code imports `AppSidebar` from `@/components/app-sidebar` — no file at that path (actual file is at `@/components/sidebar/app-sidebar`)

## Changes

### 1. Fix `src/components/sidebar/app-sidebar.tsx`
- Remove `TeamSwitcher` import and `<TeamSwitcher>` from JSX (remove from `SidebarHeader`)
- Fix import paths:
  - `@/components/nav-main` → `@/components/sidebar/nav-main`
  - `@/components/nav-projects` → `@/components/sidebar/nav-content`
  - `@/components/nav-user` → `@/components/sidebar/nav-user`

### 2. Create `src/components/app-sidebar.tsx`
Re-export file so the user's import path `@/components/app-sidebar` resolves:
```ts
export { AppSidebar } from "@/components/sidebar/app-sidebar"
```

### 3. Replace `src/app/page.tsx`
Replace entire content with the user's provided code (shadcn sidebar layout with `SidebarProvider`, `AppSidebar`, `SidebarInset`, breadcrumbs, placeholder grid).

## Files modified
- `src/components/sidebar/app-sidebar.tsx` (edit imports, remove TeamSwitcher)
- `src/components/app-sidebar.tsx` (new — re-export)
- `src/app/page.tsx` (replace content)

## Verification
- Run `npm run build` (or `next build`) to confirm no import/type errors
- Visit `/` in browser to confirm sidebar renders with NavMain, NavProjects, NavUser
